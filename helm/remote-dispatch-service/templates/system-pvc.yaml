apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.app }}-system-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: {{ .Values.app }}-system-volume
  resources:
    requests:
      storage: 5Gi
