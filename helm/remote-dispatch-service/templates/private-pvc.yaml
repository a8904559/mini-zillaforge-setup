apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.app }}-private-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: {{ .Values.app }}-private-volume
  resources:
    requests:
      storage: 5Gi
