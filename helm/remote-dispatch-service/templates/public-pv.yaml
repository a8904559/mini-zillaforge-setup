apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.app }}-public-pv
spec:
  storageClassName: {{ .Values.app }}-public-volume
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  local:
    path: {{ .Values.volume.public_path }}
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: Exists