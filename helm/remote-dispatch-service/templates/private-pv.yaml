apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.app }}-private-pv
spec:
  storageClassName: {{ .Values.app }}-private-volume
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  local:
    path: {{ .Values.volume.private_path }}
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: Exists