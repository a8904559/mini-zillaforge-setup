apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.app }}-system-local-pv
spec:
  storageClassName: {{ .Values.app }}-system-local-volume
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  local:
    path: {{ .Values.systemSource.path }}
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: Exists